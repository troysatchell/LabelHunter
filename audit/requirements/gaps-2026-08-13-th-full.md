# Requirements gaps — LabelHunter (2026-08-13, commit e63b00bd3962)

Generated by the full sweep labelled `2026-08-13-th-full`. Rulings INT-004/005/006 applied.

## Requirements needing work

### TH-R2 — PARTIAL
- **Quote:** "If we can't get results back in about 5 seconds, nobody's going to use it. We learned that the hard way."
- **Source:** source-TH.md
- **Meaning in code:** Single-label verification completes in ~5 seconds wall-clock from submission to rendered result.
- **What is missing:** The figure is good news — 3834ms p50 against a 5000ms bar, measured over real HTTP against the deployed instance. It just cannot carry VERIFIED at this commit. INT-002 is unconditional: "An artifact that predates a change to the measured path is stale, and a stale artifact never supports VERIFIED." Thirteen commits touching files the artifact's own pipelineScope names as measured landed after [...]
- **Suggested scope:** One command plus one commit, and TRO-568 already removed the blocker. Set ACCESS_CODE in the environment and run `pnpm latency:check -- --url https://labelhunter-web.onrender.com` (measure.ts now sends the x-access-code header, scripts/latency/access-code.ts), then commit the regenerated scripts/latency/results/single-label-verify-url-mode.json. No code change is needed — only a fresh measurement of code that already exists and is already believed fast enough.

### TH-R3 — PARTIAL
- **Quote:** "We need something my mother could figure out—she's 73 and just learned to video call her grandkids last year, if that gives you a benchmark. Half our team is over 50. Clean, obvious, no hunting for buttons."
- **Source:** source-TH.md
- **Meaning in code:** UI is a single obvious primary flow: large controls, minimal navigation, no hidden actions; usable by low-tech-comfort users without training.
- **What is missing:** INT-006 (Troy, 2026-08-13): backlogged for a real UX/accessibility pass. Troy has not reviewed the UI himself and flagged accessibility as the reason it matters.
- **Suggested scope:** Walk the CURRENT screens as a first-time user and assess for accessibility, not only for function. Three user-facing surfaces post-date the last walkthrough: the access-code screen (never walked at all), review-queue paging, and two new error states. Automated evidence stays green; what is missing is the human judgement TH-R3's own bar names.

### TH-R6 — PARTIAL
- **Quote:** "But for a prototype? Just don't do anything crazy. We're not storing anything sensitive for this exercise."
- **Source:** source-TH.md
- **Meaning in code:** No PII or sensitive data persisted; sane baseline security (no exposed secrets, no wild permissions); scope-appropriate, documented.
- **What is missing:** Data-model half: fully met and behaviorally supported (`pnpm test` — 179 files, 2238 tests, ALL PASSED — includes src/server/auth/access-code.test.ts and src/proxy.test.ts). No table holds a person's name, email, address, or identifier; the review queue records the decision without the decider. Secrets posture is clean: nothing real in the repo, `sync: false` in render.yaml, `.env.local` gitignored. Baseline [...]
- **Suggested scope:** Ship LH-063 / TRO-484 (README), whose ticket text already names "data-handling posture (TH-R6)" as required content. The smallest change that closes this row specifically: create README.md with a short "Data handling" section stating (a) the app persists only label/application compliance fields and uploaded artwork, (b) no PII is stored anywhere — including no reviewer identity on review_queue, (c) secrets live only in Render env config and a gitignored .env.local, and (d) the deployment is protected by a shared access code, rate limits, and a daily spend cap. Roughly 10 lines of prose; every fact is already true in code and citable. Final wording is Troy's per the ticket.

### TH-R7 — PARTIAL
- **Quote:** "our network blocks outbound traffic to a lot of domains, so keep that in mind if you're thinking about cloud APIs. During the scanning vendor pilot, half their features didn't work because our firewall blocked connections to their ML endpoints."
- **Source:** source-TH.md
- **Meaning in code:** Design consciously addresses constrained-network deployment: minimal, well-known external endpoints (or local inference), graceful failure when an endpoint is unreachable, and the trade-off documented.
- **What is missing:** INT-004 (Troy, 2026-08-13): INT-003's graded-deliverable standard extends here. The implementation half is fully met; only the prose placement is outstanding.
- **Suggested scope:** Move the outbound-dependency list and its degradation behaviour from docs/error-states.md into docs/approach.md (or README.md). The content already exists in full and needs no new research. TRO-485/PR #68 already assigns it there, so this closes when that merges.

### TH-R9 — PARTIAL
- **Quote:** "the warning statement check is actually trickier than it sounds. It has to be exact. Like, word-for-word, and the 'GOVERNMENT WARNING:' part has to be in all caps and bold."
- **Source:** source-TH.md
- **Meaning in code:** Government warning verified word-for-word against the statutory text; 'GOVERNMENT WARNING:' prefix checked for all-caps; bold checked, with a verdict consequence. Strict where TH-R8 is lenient — the two matching regimes coexist deliberately. (Corrected 2026-08-13 per INT-005: this field previously read "bold/formatting checked **or explicitly documented as a limitation**". That allowance appears nowhere in the source quote, which reads "has to be in all caps and bold" and describes rejecting a label for getting the caps wrong. An interpretation may narrow a requirement into something testable; it may never widen it into something weaker than the brief.)
- **What is missing:** INT-005 (Troy, 2026-08-13): the inventory's Meaning-in-code previously allowed the bold rule to be satisfied by being 'explicitly documented as a limitation'. That allowance is absent from the source quote and has been struck. Word-for-word and all-caps remain verified against real photographs; BOLD IS CAPTURED AND NEVER CHECKED.
- **Suggested scope:** Give the captured bold signal a verdict consequence. The extractor already returns bold: true|false|uncertain (schema.ts:80, prompt.ts:50, response.ts:178) and the golden set carries bold ground truth (TRO-527). Nothing reads it. TRO-532 (LH-025 stroke-width bold advisory) and TRO-533 (LH-026 surface the bold signal) are the existing tickets; either one closes the gap. Until then a correctly-capitalised but non-bold GOVERNMENT WARNING passes, which the brief calls a rejection.

### TH-R14 — MISSING
- **Quote:** "README with setup and run instructions"
- **Source:** source-TH.md
- **Meaning in code:** README covers setup + run from scratch.
- **What is missing:** There is no `README.md` at the repo root on origin/main. `git ls-files` returns exactly one README anywhere in the tree, `golden-set/README.md`, which is a golden-set design document. A fresh-clone walkthrough cannot succeed because there is nothing to walk through. MID-FLIGHT, NOT UNWRITTEN: PR #66 (branch `feat/lh-063-readme`, TRO-484 / LH-063) is open and adds `README.md` with 197 lines, already through two [...]
- **Suggested scope:** Merge PR #66. The document is written and reviewed; the only remaining cost is Troy's ruling on the G6 docs-only gate — grant the one-off exception (the `factory/gate-exceptions.json` mechanism TRO-553 built already exists) or decide docs-only tickets take a different check. One follow-up edit after merge: that README deliberately omits the deployed URL and access code, because PR #43 (key protection) was still open when it was written. PR #43 merged at e06f0f2 and the gate is live, so the URL and the access code can now go in, which is exactly what docs/PRD.md:240 and :248 intend.

### TH-R15 — MISSING
- **Quote:** "Brief documentation of approach, tools used, assumptions made"
- **Source:** source-TH.md
- **Meaning in code:** Docs section covering approach, tool choices, and assumptions.
- **What is missing:** `docs/approach.md` does not exist on origin/main (`ls docs/` returns deploy.md, error-states.md, PRD.md, prd-brainstorm-questions.md, reference-photo-provenance.md, and four subdirectories). INT-003 already settled that the raw material sitting in `docs/PRD.md`, `docs/error-states.md` and `docs/deploy.md` does not close this row: "An evaluator opens those. Nobody opens `docs/deploy.md`. A requirement met by [...]
- **Suggested scope:** Merge PR #68. The 259-line document is already written against `audit/requirements/gaps.md`'s assembly instructions; nothing further needs authoring. Same single blocker as TH-R14 — Troy's call on the G6 regression-test gate for a docs-only branch. One check to make before merging, because it is the specific failure mode `audit/requirements/gaps.md` warns about: confirm the document cites the eval band (extraction 87.22-87.78%, cascade-verdict 80.56-83.33%, K=3, N=36) and never a single point value inside it.

### TH-R16 — PARTIAL
- **Quote:** "Deployed Application URL" / "Working prototype we can access and test"
- **Source:** source-TH.md
- **Meaning in code:** Live deployment reachable by evaluators; core flow works there, not just locally.
- **What is missing:** HALF ONE HOLDS, HALF TWO DOES NOT. Holds — the URL loads. The instance is live and answering right now: `/` returns 307 to `/access-code`, `/access-code` returns 200, `/api/health` returns 200. The access-code gate in front of it is deliberate (docs/PRD.md:248), not a defect. Does not hold — "single-label verify succeeds on the deployed instance" was not proved against the code that ships at e63b00b. The only [...]
- **Suggested scope:** Two steps, neither needing new code. (1) Re-run the deployed-target harness ONCE with the credential set: `ACCESS_CODE=<live value> pnpm latency:check -- --url=https://labelhunter-web.onrender.com --runs=20`, then commit the fresh artifact. TRO-568 (merged at e63b00b, `scripts/latency/access-code.ts`) added the `x-access-code` header support that made this run impossible before, so the blocker is gone. One run only — each request bills a real Anthropic call and spends the target's per-IP rate-limit budget. That same run also clears TH-R2's staleness, so it closes two rows at once. (2) Merge PR #66 so the access code reaches an evaluator, per docs/PRD.md:248. TRO-483's golden-set seeding improves the demo but is not what holds this row below VERIFIED.

### TH-R19 — PARTIAL
- **Quote:** "Appropriate technical choices for the scope"
- **Source:** source-TH.md
- **Meaning in code:** Rubric line — stack sized to a prototype, not over- or under-engineered; choices defended in docs.
- **What is missing:** approach'` returns nothing at HEAD e63b00b. No document in the repo is the approach doc. WHAT IS PRESENT AND STRONG. The technical choices are sized to a prototype and defended in three independent places. (1) Scope-appropriate stack: package.json lists exactly nine runtime dependencies (Next, React/React-DOM, drizzle-orm, pg, sharp, tesseract.js, fflate, @anthropic-ai/sdk) — no Redis, no queue library, no [...]
- **Suggested scope:** Write `docs/approach.md` (TRO-485 / LH-064). No code change closes this — every input already exists and needs assembling, not discovering: PRD §3.1 (lines 53-74) for the cascade rationale, PRD §3.6 (lines 132-138) for the stack, PRD §3.7 (lines 140-163) for the upgrade ladder, package.json's nine-dependency list for the scope-sizing argument, and `scripts/eval/results/benchmark-report.json` for the cascade-vs-Sonnet-only numbers (cite them as measured on 2026-08-12 with the 32-case set, not as current). One writing session.

### TH-R21 — PARTIAL
- **Quote:** "Attention to requirements"
- **Source:** source-TH.md
- **Meaning in code:** Rubric line — the buried interview requirements (5s, batch, fuzzy vs strict matching, UX benchmark) are all visibly addressed.
- **What is missing:** WHAT IS ADDRESSED, each traced to code. (1) 5 seconds — a dedicated latency harness measures the exact function the route calls, against a live model, and refuses to mock (scripts/latency/measure.ts:2-16); the budget is broken into per-stage targets at PRD §3.8. (2) Batch — a real Postgres-backed queue with a concurrent worker pool, plus batch UI and E2E coverage (src/server/batch-queue/pool.ts:3, [...]
- **Suggested scope:** Two small, independent changes. (1) Surface the bold signal (TRO ticket LH-026 already exists): render `formatting.bold` as an advisory line in `src/app/_components/DetailView.tsx` next to the warning row, worded as a signal that never changes the verdict — roughly the same shape as the existing `resolved-by-sonnet` block at DetailView.tsx:94-97, plus one test. (2) Add a short 'Requirements addressed / explicitly out of scope' table to the README when TRO-484 writes it, listing the four buried interview requirements and naming bold detection and COLA integration as deliberate limits. Neither is large; the second is pure writing.

### TH-R23 — PARTIAL
- **Quote:** "A working core application with clean code is preferred over ambitious but incomplete features. Document any trade-offs or limitations."
- **Source:** source-TH.md
- **Meaning in code:** Two obligations: (a) prioritization — ship the working core before stretch features; (b) a written trade-offs/limitations section.
- **What is missing:** approach'` returns nothing. Genuinely good trade-offs content exists and is unusually candid — docs/deploy.md:84 'Known limitations' openly marks the live deploy itself as unverified, and docs/error-states.md:154 states the network-failure trade-off plainly and names what the app deliberately does not distinguish. Both are internal working documents. Under INT-003 that content does not count, and the row stays [...]
- **Suggested scope:** TRO-485 / LH-064, writing only — no code change closes this. Add a 'Trade-offs and limitations' section to `docs/approach.md` (and a short version in the README from TRO-484) by lifting content that already exists: docs/error-states.md:154-169 (the one-error-state network trade-off), docs/deploy.md:84-142 (known limitations, including the unverified-deploy caveat), and docs/PRD.md:38-49 (the explicitly-bounded list). Add the two limitations those three sources do not yet cover in one place: bold detection is extracted but not surfaced or scored, and the accuracy figures are a measured band rather than a point value.

## Orphan tickets

- None reported; ticket mapping was keyword-derived and too coarse to assert orphanhood.
