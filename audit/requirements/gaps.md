# Requirements gaps — LabelHunter (2026-08-13, commit 06dceb193f6e5)

Generated by the compare sweep labelled `2026-08-13-postmerge`. 2 rows are not VERIFIED, both
PARTIAL, neither MISSING. Ten rows closed since the last full sweep (commit e63b00b): the README
and approach doc both merged, the latency figure was re-measured fresh, and a human UX/
accessibility walkthrough ran for the first time. See `audit/requirements/REPORT.md` for the full
matrix and the row-by-row delta.

## Unticketed requirements

None. Every gap below already has a Linear ticket in project LabelHunter.

## Requirements needing work

### TH-R9 — PARTIAL
- **Quote:** "the warning statement check is actually trickier than it sounds. It has to be exact.
  Like, word-for-word, and the 'GOVERNMENT WARNING:' part has to be in all caps and bold."
- **Source:** source-TH.md, L33 (Jenny Park interview)
- **Meaning in code:** Government warning verified word-for-word against the statutory text;
  'GOVERNMENT WARNING:' prefix checked for all-caps; bold checked, with a verdict consequence.
- **What is missing:** Word-for-word and all-caps are both verified against real photographs
  (INT-001's condition). Bold is not: the extractor reads a `bold` signal
  (`src/server/extractor/types.ts:80`) for every label, and the value is validated into the
  response, but no router or comparator code reads it — a correctly worded, correctly
  capitalized, non-bold `GOVERNMENT WARNING:` prefix still passes today. Re-confirmed this sweep
  with a fresh `grep` (0 hits downstream of the extractor), not carried forward from the prior
  sweep unchecked. `docs/approach.md` now names the gap plainly ("a real gap, not a soft one"),
  correcting an earlier draft that had understated it as a low-confidence signal.
- **Suggested scope:** TRO-532 (LH-025, stroke-width bold advisory check) or TRO-533 (LH-026,
  surface the bold signal) — either closes the gap. Both are Todo in Linear, unstarted as of this
  sweep. TRO-569 (Urgent, filed by a peer session earlier today) also tracks this directly.

### TH-R7 — PARTIAL
- **Quote:** "our network blocks outbound traffic to a lot of domains, so keep that in mind if
  you're thinking about cloud APIs. During the scanning vendor pilot, half their features didn't
  work because our firewall blocked connections to their ML endpoints."
- **Source:** source-TH.md, L22 (Marcus Williams interview)
- **Meaning in code:** Docs name every outbound dependency and the degradation/failure behavior
  when it is blocked.
- **What is missing:** README.md now names the one outbound dependency directly ("LabelHunter
  makes exactly one outbound call to a public vendor API while it runs: the Anthropic API") —
  real progress since the last sweep, when neither README.md nor docs/approach.md existed at all.
  But the "what happens when it is blocked" content is still not reproduced in either graded
  deliverable — README.md only links to `docs/error-states.md` ("See docs/error-states.md for the
  full dependency table and what happens if the Anthropic endpoint is unreachable"), and
  docs/approach.md, read in full this sweep, does not mention the scenario at all. INT-004 already
  ruled that this content must appear IN a graded deliverable, not be pointed at from one.
- **Suggested scope:** Copy (not link) `docs/error-states.md`'s dependency table (lines 17-37,
  including the "If blocked or unreachable" column) into `docs/approach.md`. The content already
  exists, is already correct, and is already proven behaviorally
  (`src/app/api/verify/route.test.ts:397` — a real `APIConnectionError` yields a designed 503 with
  a retry affordance). This is a short, mechanical documentation move, not new engineering or
  research.

## Rows that closed this sweep

For the record — not gaps, but the ten rows that moved to VERIFIED and why, so a reader does not
have to reconstruct it from the delta table alone:

- **TH-R14, TH-R15** (README.md, docs/approach.md) — both merged (PR #66, PR #68). TH-R14 was
  additionally confirmed with a literal fresh-clone install/migrate/build check this sweep, not
  just the file's existence.
- **TH-R6, TH-R19, TH-R23** — each was already fully justified in code/internal docs; the only gap
  was that the justification lived nowhere a grader would open. README.md and docs/approach.md now
  carry it directly.
- **TH-R2, TH-R16** — a fresh, unstale latency artifact (measuredAt 2026-08-13T19:43:22Z, p50
  3618ms, 20/20 PASS against the live deployed instance) replaced the stale figure the last sweep
  held at PARTIAL under INT-002.
- **TH-R3** — TRO-570's first human walkthrough of every current screen, including the
  never-before-walked access-code screen, plus a full accessibility pass (axe-core, contrast,
  keyboard, structure), found zero violations. Closes the gap INT-006 named.
- **TH-R5** — this sweep ran and captured the `grep -rn "COLA" src/` check directly (1 hit, a
  regulatory citation, not a client), accepting it as the behavioral artifact the prior sweep left
  as an open option.
- **TH-R21** — a judgment call, flagged as such in `REPORT.md`'s Blocked/assumed section: every
  row in this inventory is now either VERIFIED or PARTIAL-with-an-explicit-ticketed-gap, and
  `docs/approach.md` itself points readers to this audit trail rather than hiding the two open
  rows (TH-R7, TH-R9).

## Orphan tickets

None reported. Ticket mapping is keyword-derived against 108 issues in the LabelHunter project —
too coarse a population to assert genuine orphanhood confidently, the same reasoning the prior
sweep used.
