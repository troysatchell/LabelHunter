# Requirements gaps — LabelHunter (2026-08-13, commit 8f14c17fbf5878)

Generated by the compare sweep labelled `2026-08-13-postreskin`. **1 row is not VERIFIED** —
TH-R9, PARTIAL. TH-R7 closed during this sweep (TRO-574 merged mid-run). Every other row in the
23-requirement inventory is VERIFIED. See `audit/requirements/REPORT.md` for the full matrix.

## Unticketed requirements

None. The one open gap already has Linear tickets in project LabelHunter.

## Requirements needing work

### TH-R9 — PARTIAL (the only open row)
- **Quote:** "the warning statement check is actually trickier than it sounds. It has to be exact.
  Like, word-for-word, and the 'GOVERNMENT WARNING:' part has to be in all caps and bold."
- **Source:** source-TH.md, L33 (Jenny Park interview)
- **Meaning in code:** Government warning verified word-for-word against the statutory text;
  'GOVERNMENT WARNING:' prefix checked for all-caps; bold checked, with a verdict consequence.
- **What is missing:** Word-for-word and all-caps are both verified against real photographs
  (INT-001's condition). Bold is not: the extractor reads a `bold` signal
  (`src/server/extractor/types.ts:80`) for every label, and the value is validated into the
  response, but no router or comparator code reads it — a correctly worded, correctly
  capitalized, non-bold `GOVERNMENT WARNING:` prefix still passes today. Re-confirmed this sweep
  with a fresh `grep` (0 hits downstream of the extractor).
- **Suggested scope:** TRO-532 (LH-025, stroke-width bold advisory check) is now In Progress —
  no PR open yet as of this check. TRO-533 (LH-026, surface the bold signal) remains Todo;
  either ticket closes the gap. TRO-528 (bold-isolating golden cases) supports the eventual eval
  coverage. TRO-569 (Urgent) tracks the gap itself.

## Closed during this sweep

**TH-R7** — TRO-574 (PR #81) merged while this sweep was in progress. The outbound-dependency
table and its "if blocked or unreachable" column now live directly in `docs/approach.md`'s new
"Outbound dependencies and degradation" section, and README.md points to that reproduced
content instead of the internal `docs/error-states.md` working document. INT-004's rule (content
must be IN a graded deliverable, not linked from one) is now fully met.

## Orphan tickets

Not attempted this sweep — ticket population was not re-pulled from scratch (see REPORT.md's
Coverage and limitations). The prior sweep's own reasoning (108 issues is too coarse a
population for keyword-derived orphan detection to mean anything) still applies.
