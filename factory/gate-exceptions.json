{
  "$comment": [
    "Human-approved exceptions to a gate.sh check. TRO-553 built this after G6",
    "(regression-test) failed three real docs-only/test-only tickets whose",
    "premise — every ticket changes production code — does not hold for them.",
    "",
    "RULES:",
    "(1) Only the orchestrator writes an entry here, and only after Troy's",
    "    approval already exists on the named Linear ticket. An agent must",
    "    never add its own entry — that would be self-approval.",
    "(2) gate.sh honors an entry ONLY when \"approver\" is a non-empty string.",
    "    An entry with an empty or missing approver reads as unapproved, and",
    "    the gate still fails — see scripts/factory/gate-exceptions.ts.",
    "(3) One entry exempts one ticket from one named gate id. It does not",
    "    exempt the ticket from any other gate, and it does not carry over to",
    "    a different ticket that happens to touch the same files.",
    "",
    "Schema per entry: ticket (TRO-nnn), gate (the gate.sh check id), reason",
    "(why no red-first case is possible), approver (a name; empty means NOT",
    "approved), date (YYYY-MM-DD the approval was given), pr (the PR this",
    "exception covers, for provenance)."
  ],
  "version": 1,
  "exceptions": [
    {
      "ticket": "TRO-547",
      "gate": "regression-test",
      "reason": "Test-repair ticket: the flake fix has no production-code change to write a red-first case against, and the only behavior candidate is already covered by BatchProgressBrowser.test.tsx's existing test. Flagged in CHANGES.md and escalated by the TRO-547 agent rather than padding the suite.",
      "approver": "Troy",
      "date": "2026-08-13",
      "pr": 50
    },
    {
      "ticket": "TRO-472",
      "gate": "regression-test",
      "reason": "LH-CP3 checkpoint 3 walkthrough material: docs only, no production or test code changed.",
      "approver": "Troy",
      "date": "2026-08-12",
      "pr": 18
    }
  ]
}
